# 项目协作约定

## 工作方式（重要）
- **改代码一律先在这个本地目录改**，改完在本地验证（语法、接口、单测）。
- **不要自行部署到服务器。** 只有用户明确说"部署/上线"时才部署。
- 部署前先备份服务器上的被覆盖文件。

## 代码风格
- 代码要紧凑，不要堆空行。`app.py` 是 `make_compact.py` 压过的风格：顶层 `def` / `@app.` 之间不空行，全文件不出现连续两行空行。
- 前端以 `front.html` 为准，后端以 `app.py` 为准（app.py 首页路由直接读 `front.html`，无需再同步副本）。

## 部署目标（用户明确要求时才执行）
- 服务器 `10.2.248.34`（root 可密钥登录：`~/.ssh/id_ed25519`；另有 algo 账号，密码见私密记录，**不写入仓库**）。
- 部署目录 `/opt/ad_mining`，服务 `ad_mining.service`（`/venv/bin/uvicorn app:app --port 8009`，`WorkingDirectory=/opt/ad_mining`）。
- 重启：`systemctl restart ad_mining`（需 root）。
- 数据根 `/mnt/Data_Platform/zhj_datamining/test`（NAS）。**2026-09-18 起 index.faiss / metadata.json 已迁本地盘**
  `/opt/ad_mining/index_store/<项目>/`（`AD_INDEX_STORE` 可改）：CIFS 不允许 rename（非原子写，欧洲事故根因）
  且顺序读仅 110MB/s，本地 NVMe 2658MB/s 且支持原子替换。NAS 上的同名文件是**旧版遗留**，
  本地缺失时服务会自动回迁（见 `_load_project_context_cold`）；帧图仍在 NAS。
- **每日备份**：`/etc/cron.d/ad_mining_backup`（03:30）跑 `/opt/ad_mining/scripts/daily_backup.py`
  —— mining.db VACUUM INTO（`/opt/ad_mining/backups/db/`，留 7 份）+ 索引快照（`backups/index_store/`）
  + 三方对账（ntotal==metadata==DB assets，不一致打 ⚠️）。日志在 `logs/backup.log`。
- **重启后自动续跑**：被重启打断的抽帧任务自动重提（只认 `_cleanup_stale_jobs` 的「服务重启中断」标记，
  人工中止的不碰；1 小时防抖；`AD_AUTO_RESUME=0` 关闭）。TAG/向量化不在自动续跑范围（需人工）。

## 部署踩过的坑（务必注意）
1. **服务器 Python 是 3.8.10，本地是 3.14。** 上传前必须用服务器解释器校验：
   `/venv/bin/python -m py_compile <file>`，否则可能语法不兼容。
2. **服务器 `gpu_patch.py` 比本地新**（有 ultralytics "Both events must be recorded" 计时 bug 的三次重试逻辑），不要覆盖。`db_service.py`、`gpu_manager.py` 也留在服务器。
3. **NAS 是 CIFS 挂载（账号 `ningbo_new`），能建文件、不能删文件**：服务器侧连自己刚建的文件 `rm` 都 Permission denied。清理类操作不要指望服务器侧完成。
4. 本地 Windows 与服务器之间约有 **8 分钟时钟偏差**，看日志时间戳注意。
5. 另有一台 `192.168.20.206`（NAS 本体），SSH 公钥登录被拒。

## 变更原则（重要，用户明确要求）
- **稳定优先，兼顾效率**：任何提速/并行/批量类改动，先确认不会挤爆资源（CPU / 内存 / 磁盘 / 网盘 I/O）再上。宁可慢一点，不能把服务或别人的工作打挂。
- **资源要全看，不能只看一个指标**：曾只凭 CPU 占用（47%->91%）就判断"8 路更好"，忽略了向量化 DataLoader 的共享内存（单任务几 GB、多任务叠加），结果把 31G 机器打爆，OOM 连 VS Code / 飞书一起被杀，抽帧停了 10 小时。
- **提速类改动的流程**：先本地/小样本实测关键指标（内存峰值、IO、吞吐）→ 保留可一键回退的开关与备份 → 部署后立刻验证 → 出问题马上回退。默认值取保守值（如抽帧并行默认 1 路）。
- **大目录操作要预估规模**：几万视频/图片的目录，扫描、列举、一次性载入都会严重退化，必须分块处理或加进度显示，避免"看着像卡死"。

## 数据去重约定（用户 2026-09-19 明确要求，遍历/导入必须遵守）
- **混合目录**：一个文件夹里既有视频又有图片 → **只处理视频，图片跳过**（图片通常是同一内容的连拍，入库即重复）。
  视频抽帧/守护链路天然只走视频 ✓；直导 import_from_path 已按此修改（目录含视频则该目录图片不导入）。
- **2M/8M 双机位**：同车同日期的 2M 和 8M 目录内容一样 → **只遍历 2M，8M 跳过**（2M 更小更快）。
  auto_ingest.py 已实现（`_has_2m_sibling`，8M 目录存在 2M 兄弟时整目录跳过并记清单 skip_8m）。
- **同一台车/同一文件夹**：内容只进一次（清单 manifest 按视频路径去重）。
- default 项目已按用户要求**全删**（2026-09-19，含作废目录数据与跨项目重复帧）；tag 备份表
  `assets_ai_tags_backup_20260919` 保留可查。default 保留项目行但数据为空，新数据进各自项目。

## VLM 打标必须用 7B（用户明确要求，硬规则）
- **规则**：VLM 打标（AI 场景分析 / Clip 判定）**只认 7B**（`Qwen/Qwen2-VL-7B-Instruct`，4bit）。
  跑 VLM 前必须确认实际加载的是 7B。**非 7B 要经人工确认后才能跑**，不允许静默降级。
- **怎么判**：`GET /api/vlm_status` 返回 `{model, is_7b, required_7b, ok}`；日志里看
  `[VLM] 实际规模: hidden=3584`（7B）而不是 `hidden=1536`（2B）。前端点「AI场景分析」会先查这个接口。
- **已加的两道拦**：① 后端 `run_ai_batch` 在非 7B 时直接返回 409 拒绝建任务，
  除非显式传 `vlm_confirm=1`（=人工确认过"非 7B 也要跑"）；② 前端此时会弹确认框。
- **为什么**（2026-09-18 查实，别再犯）：加载链会**静默降级** —— 首选名 `AD_VLM_MODEL` 默认是
  Qwen2.5-VL-3B，而服务器 `transformers` 是 4.46.3（导不出 `Qwen2_5_VLForConditionalGeneration`），
  这一支必然抛异常；接着显存判定不足又跳过 7B，最终落到 **2B**，而 `ai_tags` 里记的仍是"7B"。
  于是 2B 的判定被当成 7B 的结果在用：**"行人横穿"占全部事件 55%（9,672/17,559）**、
  63% 的事件没有 confidence、69% 的事件证据链（F1→中间帧→FN）整批丢失。
- **附带修好的**：`max_new_tokens` 原来多帧 220/单帧 160，输出连结尾 `}` 都吐不到就被截断 →
  JSON 解析失败 → 退回关键词兜底 → **evidence/confidence 就是在这里丢的**。现在改成
  `_vlm_budget()`（多帧 512 / 单帧 384，可用 `AD_VLM_MAX_TOKENS*` 覆盖）。
- **别再犯的动作**：换模型/改 transformers 版本后，**必须先调 `/api/vlm_status` 或看启动日志
  确认 hidden=3584**，再发起打标；不要相信 `ai_tags` 里记的 model 名（那可能只是"打算加载"的名字）。
- **要真正用上 Qwen2.5-VL 系列**（含缓存里已有的 `Qwen2.5-VL-7B-Instruct-AWQ`，约 4~5G 更省显存）：
  需把 `transformers` 升到 >=4.57。**升级会牵动 SigLIP/DINO 的加载路径，属高风险改动**，
  要单独开窗口做、先备份、并准备一键回退。

## 上下文管理（用户明确要求：超过 80% 自动压缩）
- **触发条件**：当上下文使用量达到约 80% 时（或明显感觉到接近上限时），**立即**执行下面的压缩流程，不要等到撑满被截断。
- **压缩流程**：
  1. 把当前会话的要点压缩成一份**交接文档**，写到 `STATE.md`（本地笔记，已在 .gitignore，不入库），结构固定为：
     ① 部署速查（服务器/目录/服务/重启/校验/备份）② 当前状态（任务、内存、数据与库的落差）
     ③ 本次已完成 ④ 待办清单 ⑤ 常用检查命令 ⑥ 踩过的坑（每条含"别再犯"的动作）。
  2. 在本节下方**列出可交接文档**（路径 + 一句话摘要 + 更新时间），方便接续时一眼找到。
  3. 交接文档里必须写清**未完成的动作和它的判定方法**（例如"查 X 日志、核对 Y 数量"），而不是只写"待办：继续"。
- **接续时**：先读 `AGENTS.md`（本文件）+ `STATE.md`，再动手；不要把已确认过的结论重新推导一遍。
- **禁止**：把大段业务数据、密码、token 写进交接文档（本仓库是 public）。

### 可交接文档
- `STATE.md` — 当前会话状态交接（部署速查 / 当前状态 / 已完成 / 待办 / 检查命令 / 踩过的坑）。最近更新：2026-09-17。
