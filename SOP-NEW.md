# SOP：从图片导入到交付（新版 · Maxieye 场景挖掘终端）

> 对应新版 `app.py`（端口 **8009**，SigLIP + Grounding DINO-Base + YOLOv8x，**多项目隔离**）。
> 访问入口：**http://localhost:8009**

---

## 一、整体链路概览

```
[原始视频] ──视频抽帧──► [原始图片]
                          │
          ┌───────────────┴───────────────┐
          │  ① 选项目 → 导入图片入底库     │
          │   上传 / 文件夹 / 服务器路径    │
          └───────────────┬───────────────┘
                          ▼
       workspace/projects/<项目名>/  (images + index.faiss + metadata.json)
                          │
          ┌───────────────┴───────────────┐
          │  ② 高维特征去重（可选）        │
          └───────────────┬───────────────┘
                          ▼
          ┌───────────────────────────────┐
          │  ③ 语义挖掘 / 检索目标场景      │
          │  文本搜图 │ 文件名 │ 以图搜图    │
          └───────────────┬───────────────┘
                          ▼
          ┌───────────────────────────────┐
          │  ④ 目标检测预标（YOLO / DINO） │
          └───────────────┬───────────────┘
                          ▼
          ┌───────────────────────────────┐
          │  ⑤ 勾选 → 标注导出 JSON        │
          │     （交付给标注/下游）          │
          └───────────────────────────────┘
```

---

## 二、分步 SOP

### 阶段 0 — 环境准备（一次性）

1. 安装依赖：`.venv\Scripts\activate` 后 `pip install -r requirements.txt`。
2. 确认根目录有 `yolov8x.pt`。
3. 已配置 **hf-mirror.com** 镜像（`app.py` 顶部 `HF_ENDPOINT`），首次启动会从镜像下载 SigLIP 与 DINO 权重。
4. 启动：双击 `start.bat` 或 `python app.py`，浏览器打开 **http://localhost:8009**。
5. 模型在 `startup` 阶段装载（SigLIP → DINO → YOLO），期间端口暂不响应，**请耐心等待就绪**。

### 阶段 1 — 选择 / 新建项目空间

- 引导页「请选择要进入的项目空间」下拉，或点「+ 新建项目」创建。
- 每个项目相互隔离；`default` 为系统保留项目，不可删除、不可重命名。
- 进入后顶部右上角可切换项目 / 重命名（✏️）/ 删除（🗑️）。

### 阶段 2 — 图片入库（建向量底库）

三种方式任选：

- **方式 A · 前端上传**：点文件选择或「选择前端文件夹」，多选图片（或 zip）。
  - 前端自动按 300 张/批分片 `POST /api/upload_batch`，含 zip 解包；
  - 后端逐张存图 → SigLIP 批量提特征 → 入 FAISS → 落盘，进度条实时显示。
- **方式 B · 服务器/挂载路径直导**：点「从服务器/挂载网盘直导」。
  - 输入服务器绝对路径（如挂载网盘），后端 `os.walk` 递归复制新图片并向量化，适合大批量。
- **方式 C · 增量向量化**：图片已放好但未索引时，点「增量向量化底库」
  - 调用 `POST /api/build_index_online`，仅处理未索引图片。

> 顶部「原始数据池 / 已处理 / 待处理」三指标可观察入库进度。

### 阶段 3 — 高维特征去重（可选，推荐）

1. 「高维特征降重引擎」设置相似度阈值（默认 0.95）。
2. 点「执行特征矩阵扫描」`POST /api/dedup_stats`：
   - 全量矩阵乘得两两相似度 → 聚类 → 每簇保留首张，其余为冗余。
3. 结果面板显示：总帧数 / 唯一有效帧 / 冗余重复帧 / 整体冗余率。
4. 可选：导出冗余清单（TXT），或「永久粉碎并同步底库」`POST /api/delete_and_sync`
   （物理删冗余图 → 重建 FAISS 索引 → 落盘）。

### 阶段 4 — 语义挖掘 / 检索目标场景

- **文本搜图**：输入自然语言场景（如 `rainy night highway`、`overturned truck`），设 Top K，回车或点「跨模态文本搜图」`GET /api/search`。
- **文件名检索**：输入文件名关键字 `POST /api/search_by_filename`。
- **以图搜图**：多选参考图 →「执行多图寻源」`POST /api/search_by_external_image`（OR 联合，默认相似度≥0.65）。
- **浏览**：默认 `browse` 模式分页看全库（30 张/页，悬浮页码跳转）。
- 用「SigLIP 匹配度」滑块过滤检索结果。

### 阶段 5 — 目标检测预标

1. 确定目标集合（当前页 / 检索全集 / 去重簇）。
2. **YOLOv8x**：点「启动 YOLOv8x 检索」`POST /api/yolo_detect`（COCO 80 类）。
3. **Grounding DINO**：填开放词汇提示词（如 `traffic cone`）→「启动 DINO-Base 检索」`POST /api/ground_detect`。
4. 前端按「DINO 置信度」滑块过滤有效框，自动生成标签（如 `car 3`），**含有效目标的卡片自动勾选**。
5. 单张点击放大可查看叠加检测框。

### 阶段 6 — 标注导出 / 交付

1. 手动为卡片勾选 / 取消；也可点卡片「Add Tag」追加自定义标签（自动标记为已勾选）。
2. 导出：
   - `exportData(true)`：导出**已勾选**卡片 JSON；
   - `exportData(false)`：导出**未勾选**卡片 JSON；
   - `exportSearchResults()`：导出当前粗筛结果 JSON（SigLIP）。
   - 导出 JSON 结构：`{id, filename, url, score, annotations:{boxes/labels/scores}, userTags}`。
3. **交付下游**：把导出的 JSON 交给标注/质检团队，按 `id` / `filename` / `url` 取图并依据 `userTags`、`annotations` 加工，完成交付闭环。

---

## 三、关键注意事项

- **多项目隔离**：所有操作需在正确项目空间内执行，检索/去重/检测结果严格基于当前项目。
- **不要手动增删** `workspace/projects/<项目>/images/` 内文件，否则 ID 错位。
- **删图一律走**「永久粉碎并同步」或项目删除，保证索引与磁盘一致。
- 入库有进度条，单张失败不中断整批；首次装载模型较慢，属正常。
- 前端静态资源 `/static_root/TT.jpg` 与目录 `TT.png` 命名可能不一致——若页面加载 logo/背景异常，请将文件命名为 `TT.jpg` 或调整 `前端.html` 引用。

---

## 四、文件职责速查

| 文件 | 作用 |
|------|------|
| `app.py` | FastAPI 引擎（8009）：导入/检索/去重/检测/导出/多项目管理 |
| `前端.html` | 暗色科幻风单页前端（引导页 + 主应用），由后端托管 |
| `yolov8x.pt` | YOLOv8x 权重 |
| `workspace/projects/<项目>/` | 每项目独立的 images + index.faiss + metadata.json |
| `start.bat` / `start_linux.sh` | 启动脚本（激活 .venv 后运行 app.py） |
| `视频挖掘处理/视频抽帧.py` | 视频 → 帧（阶段 1 前置，可选） |
| `copy_valid.py` | 导出 JSON → 复制到交付目录（可选交付归集） |
